apiVersion: v1
kind: Service
metadata:
  name: {{ include "config-center.fullname" . }}
  labels:
    {{- include "config-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "config-center.selectorLabels" . | nindent 4 }}
